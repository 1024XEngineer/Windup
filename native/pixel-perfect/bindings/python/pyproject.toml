[build-system]
requires = ["maturin==1.14.1"]
build-backend = "maturin"

[project]
name = "windup-pixel-perfect-native"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
bindings = "pyo3"
module-name = "windup_pixel_perfect_native"
