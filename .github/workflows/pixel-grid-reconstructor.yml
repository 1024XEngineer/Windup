name: Pixel grid reconstructor CI

on:
  push:
    paths:
      - "native/pixel-perfect/crates/reconstructor/**"
      - ".github/workflows/pixel-grid-reconstructor.yml"
  pull_request:
    paths:
      - "native/pixel-perfect/crates/reconstructor/**"
      - ".github/workflows/pixel-grid-reconstructor.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: native/pixel-perfect/crates/reconstructor
    steps:
      - uses: actions/checkout@v7

      - name: Rust format
        run: cargo fmt --check

      - name: Rust tests
        run: cargo test --release --locked
