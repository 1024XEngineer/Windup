name: Pixel grid detector CI

on:
  push:
    paths:
      - "backend/native/pixel_grid_detector/**"
      - ".github/workflows/pixel-grid-detector.yml"
  pull_request:
    paths:
      - "backend/native/pixel_grid_detector/**"
      - ".github/workflows/pixel-grid-detector.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/native/pixel_grid_detector
    steps:
      - uses: actions/checkout@v7

      - name: Rust format
        run: cargo fmt --check

      - name: Rust tests
        run: cargo test --release --locked
